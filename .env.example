# AuraFit app — copy to .env and fill in values

# Required — Supabase Dashboard → Settings → API → anon public
VITE_SUPABASE_URL=https://hhgxmupzodiiqgqifmaz.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# ---------------------------------------------------------------------------
# NEVER put these in .env — Vite bundles .env into the iPhone app:
#   OPENAI_API_KEY, USDA_API_KEY, SUPABASE_SERVICE_ROLE_KEY
#
# Set server secrets in Supabase Dashboard only:
#   https://supabase.com/dashboard/project/hhgxmupzodiiqgqifmaz/functions/secrets
#     OPENAI_API_KEY  (required for AI food parsing)
#     USDA_API_KEY    (optional — free at fdc.nal.usda.gov/api-key-signup.html)
# ---------------------------------------------------------------------------
